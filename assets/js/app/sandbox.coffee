define (require) ->
	$ = require('jquery')
	#sandbox = require('cs!vendor/sandbox')
	sandboxjs = require('vendor/sandboxjs')
	
	sandbox =
		init: ->
			console.warn "using the sandbox"
		
		
	
	#end
	