define (require) ->
	# numbers = [1, 5, 10, 15, 2, 4, 6]
	# results = numbers.map (x) -> x * .5
	# 
	# 
	# console.log "numbers: ", results
	# 
	# passengers = [ ["Thomas", "Meeks"], 
	#                    ["Gregg", "Pollack"], 
	#                    ["Christine", "Wong"], 
	#                    ["Dan", "McGaw"] ];
	# 
	# modifiedName = passengers.map (name) ->
	# 	"#{name[0]} #{name[1]}"
	# 
	# console.log "names: ", modifiedName
	# 
	# 
	# console.log Math.pow 2,3
	# 
	# 
	# func = (a) ->
	# 	a + 10
	# 
	# console.log func(5)
	
	
	console.warn "sandbox.coffee is commented"
	# addMyClass = (that) -> 
	# 	$(that).toggleClass 'exed'
	# 
	# removeMyClass = (that) -> 
	# 	$(that).toggleClass 'together'
	# 
	# $('.open-and-close').click ->
	# 	isOn = $(@).hasClass 'together'
	# 	if !isOn
	# 		$(@).addClass "together"
	# 		setTimeout addMyClass, 500, [@]
	# 	else
	# 		$(@).removeClass "exed"
	# 		setTimeout removeMyClass, 500, [@]